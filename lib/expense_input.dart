import 'package:flutter/material.dart';

class ExpenseInput extends StatelessWidget {
  const ExpenseInput({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
